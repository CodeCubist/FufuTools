<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="FufuTools.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FufuTools"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="FufuTools">

    <Grid Loaded="Grid_Loaded">
        <Grid.Resources>
            <Storyboard x:Name="BgFadeInStoryboard">
                <DoubleAnimation Storyboard.TargetName="BackgroundImage"
                                 Storyboard.TargetProperty="Opacity"
                                 From="0.0" To="1.0" Duration="0:0:0.5" />
            </Storyboard>

            <AcrylicBrush x:Key="ButtonAcrylicBackground" 
                          TintOpacity="0.3" 
                          TintColor="#1A1A1A"
                          FallbackColor="#222222"/>

            <Style x:Key="BlurredButtonStyle" TargetType="Button">
                <Setter Property="Width" Value="120"/>
                <Setter Property="Height" Value="40"/>
                <Setter Property="CornerRadius" Value="4"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Padding" Value="12,4,12,4"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid x:Name="RootGrid" Background="Transparent" CornerRadius="{TemplateBinding CornerRadius}">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal">
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="NormalBackground" Storyboard.TargetProperty="Opacity" To="1.0" Duration="0:0:0.3"/>
                                            </Storyboard>
                                        </VisualState>
                                        
                                        <VisualState x:Name="PointerOver">
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="NormalBackground" Storyboard.TargetProperty="Opacity" To="0.0" Duration="0:0:0.3">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut"/>
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                            </Storyboard>
                                        </VisualState>
                                        
                                        <VisualState x:Name="Pressed">
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="NormalBackground" Storyboard.TargetProperty="Opacity" To="0.0" Duration="0:0:0.1"/>
                                                <DoubleAnimation Storyboard.TargetName="PressedOverlay" Storyboard.TargetProperty="Opacity" To="0.2" Duration="0:0:0.1"/>
                                            </Storyboard>
                                        </VisualState>
                                        
                                        <VisualState x:Name="Disabled">
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="Opacity" To="0.5" Duration="0"/>
                                            </Storyboard>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>

                                <Border x:Name="NormalBackground" 
                                        Background="{StaticResource ButtonAcrylicBackground}" 
                                        CornerRadius="{TemplateBinding CornerRadius}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"/>

                                <Border x:Name="PressedOverlay" 
                                        Background="Black" 
                                        Opacity="0"
                                        CornerRadius="{TemplateBinding CornerRadius}"/>

                                <ContentPresenter x:Name="ContentPresenter"
                                                  Content="{TemplateBinding Content}"
                                                  ContentTransitions="{TemplateBinding ContentTransitions}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  Padding="{TemplateBinding Padding}"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  AutomationProperties.AccessibilityView="Raw"/>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/> 
        </Grid.RowDefinitions>

        <Image x:Name="BackgroundImage" Source="Assets/bg.png" Stretch="UniformToFill" Grid.RowSpan="3" Opacity="0"/>

        <Grid x:Name="AppTitleBar" Height="32" Background="Transparent" Grid.Row="0">
            <TextBlock Text="FufuTools" 
                       VerticalAlignment="Center" 
                       Margin="16,0,0,0" 
                       Style="{ThemeResource CaptionTextBlockStyle}"/>
        </Grid>
        
        <StackPanel Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Center" Spacing="20" Margin="40,0,0,0">
            <Button Content="扫码登录" 
                    Style="{StaticResource BlurredButtonStyle}"
                    Click="OpenLoginPage_Click"/>
            
            <Button Content="战绩信息"  
                    Style="{StaticResource BlurredButtonStyle}"
                    Click="OpenBBSWindow_Click"/>
            
            <Button Content="自动签到" 
                    Style="{StaticResource BlurredButtonStyle}"
                    Click="OpenCheckinPage_Click"/>
        </StackPanel>
    </Grid>
</Window>